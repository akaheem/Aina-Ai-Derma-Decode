# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase
.firebase/
.firebaserc
firebase-debug.log
firebase-functions-debug.log

# Environment
.env
.env.local
.env.*.local

# Secrets — NEVER commit (belt & suspenders; patterns above already cover these).
# functions/.env holds the YouCam API key + RSA secret; must stay server-side only.
**/.env
**/.env.local
functions/.env
functions/.env.local
# ...but DO keep the safe, value-less templates:
!.env.production.example
!**/.env.example
