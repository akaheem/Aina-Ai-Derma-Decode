# .env for LOCAL testing only (`vercel dev`). NEVER commit real values.
# In production these are set in the Vercel dashboard (Project → Settings →
# Environment Variables), NOT from this file.

# Perfect Corp / YouCam S2S credentials (server-side secrets)
YOUCAM_API_KEY=
YOUCAM_SECRET_KEY=

# Firebase project whose ID tokens are accepted (public value)
FIREBASE_PROJECT_ID=aina-ai-derma-decode
